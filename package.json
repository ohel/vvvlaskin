{
    "name": "vvvlaskin",
    "scripts": {
        "build": "npx tsc src/app.ts",
        "example": "npm run build && node src/app.js example.jsonl",
        "examplebalances": "npm run build && node src/app.js example.jsonl balances",
        "transactions": "npm run build && node src/app.js transactions.jsonl",
        "balances": "npm run build && node src/app.js transactions.jsonl balances"
    },
    "devDependencies": {
        "@types/node": "^20.11.25",
        "typescript": "^5.4.2"
    }
}
