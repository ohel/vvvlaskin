{
    "name": "vvvlaskin",
    "scripts": {
        "build": "npx tsc src/app.ts",
        "example": "npm run build && node src/app.js example.jsonl",
        "exampletotals": "npm run build && node src/app.js example.jsonl totals",
        "transactions": "npm run build && node src/app.js transactions.jsonl",
        "totals": "npm run build && node src/app.js transactions.jsonl totals"
    },
    "devDependencies": {
        "@types/node": "^20.11.25",
        "typescript": "^5.4.2"
    }
}
